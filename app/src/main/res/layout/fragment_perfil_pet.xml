<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragment.PerfilPetFragment"
    android:orientation="vertical">

    <com.mikhaellopez.circularimageview.CircularImageView
        android:id="@+id/civPerfil"
        android:layout_width="@dimen/civ_size"
        android:layout_height="@dimen/civ_size"
        android:src="@drawable/gato4"
        app:civ_border_color="@color/colorBrown"
        app:civ_border_width="@dimen/civ_border"
        app:civ_shadow= "true"
        app:civ_shadow_radius="@dimen/civ_shadow"
        app:civ_shadow_color="@color/colorBrown"
        android:scaleType="fitCenter"
        android:layout_gravity="center"
        />

    <TextView
        android:id="@+id/tvMiMascota"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/nombre"
        android:textSize="@dimen/civ_text"
        android:fontFamily="casual"
        tools:targetApi="jelly_bean"
        android:textStyle="bold"
        android:textColor="@color/colorBrown"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPerfilmascotas"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="center_horizontal"
        />

</LinearLayout>